apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: data-catalog-backend
  namespace: default
  labels:
    team: {{team}}
spec:
  image: {{version}}
  port: 8080
  ingresses:
    - "https://datacatalog.prod-sbs.nais.io/datacatalog/backend"
    - "https://www.nav.no/datacatalog/backend"
  replicas:
    min: 2
    max: 3
    cpuThresholdPercentage: 50
  liveness:
    path: datacatalog/backend/internal/isAlive
    initialDelay: 60
  readiness:
    path: datacatalog/backend/internal/isReady
    initialDelay: 60
  resources:
    limits:
      cpu: 1500m
      memory: 1024Mi
    requests:
      memory: 528Mi
  vault:
    enabled: true
  prometheus:
    enabled: true
    path: datacatalog/backend/internal/metrics
