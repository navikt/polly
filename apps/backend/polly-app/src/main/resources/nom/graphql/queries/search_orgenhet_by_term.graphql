query searchOrgEnhetByTerm($searchTerm: String!) {
    searchOrgEnhet(term: $searchTerm
        filter:  {
            gyldigTomSelection: IKKE_PASSERT
        }
    ) {
        navn
        id
        orgEnhetsType
        nomNivaa
    }
}